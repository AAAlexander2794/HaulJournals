using System;
using System.Collections.Generic;
using HaulJournalsDAL;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace HaulJournalsUnitTests
{
    [TestClass]
    public class TestDal
    {
        [TestClass]
        public class TestPartialClasses
        {
            //[TestMethod]
            //public void TestUsersClone()
            //{
            //    users originalUser = new users()
            //    {
            //        id = 1,
            //        groups = new groups(),
            //        voyages = new List<voyages>(),
            //        username = "username",
            //        password = "password",
            //        access_level = 2,
            //        note = "note",
            //        group_id = 3
            //    };

            //    users clonedUser = (users) originalUser.Clone();

            //    //Проверки

            //    Assert.AreEqual(originalUser, clonedUser);

            //    //Свойства
            //    Assert.AreEqual(originalUser.access_level, clonedUser.access_level);
            //    Assert.AreEqual(originalUser.group_id, clonedUser.group_id);
            //    Assert.AreEqual(originalUser.id, clonedUser.id);
            //    Assert.AreEqual(originalUser.note, clonedUser.note);
            //    Assert.AreEqual(originalUser.password, clonedUser.password);
            //    Assert.AreEqual(originalUser.username, clonedUser.username);

            //    Assert.AreNotSame(originalUser, clonedUser);
            //    Assert.AreNotSame(originalUser.voyages, clonedUser.voyages);
            //    Assert.AreNotSame(originalUser.groups, clonedUser.groups);
            //}


        }

       
    }
}
